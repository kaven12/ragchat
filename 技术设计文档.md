# 技术设计文档

**项目名称**: 智能文件管理与问答检索系统
**版本**: 2.0
**编写日期**: 2026-05-27
**状态**: Draft

***

## 1. 概述

本文档是智能文件管理与问答检索系统的技术设计文档，基于以下技术栈进行设计：

| 分类        | 技术选型                         | 版本     |
| --------- | ---------------------------- | ------ |
| **语言**    | Java                         | 21     |
| **框架**    | Spring Boot                  | 3.2.x  |
| **向量数据库** | Milvus                       | 2.4.x  |
| **AI框架**  | LangChain4J                  | 0.30.x |
| **LLM**   | DeepSeek                     | API    |
| **开发模式**  | Harness Agent + Skills + MCP | -      |

### 1.1 设计原则

- **安全性**: 所有接口必须身份验证，遵循最小权限原则
- **可扩展性**: 模块化设计，支持水平扩展
- **高性能**: 优化查询性能，支持高并发
- **可维护性**: 代码规范，日志完善，监控到位
- **Agent化**: 采用Harness Agent模式，支持技能组合

***

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Client Layer                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                    │
│  │   Web浏览器   │  │   移动端    │  │  Harness UI │                    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘                    │
└─────────┼────────────────┼────────────────┼────────────────────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │ HTTPS
          ┌────────────────▼────────────────┐
          │           Gateway Layer          │
          │  ┌────────────────────────────┐ │
          │  │     Spring Cloud Gateway    │ │
          │  │   - SSL终端                │ │
          │  │   - 路由转发               │ │
          │  │   - 请求限流                │ │
          │  └────────────────────────────┘ │
          └────────────────┬────────────────┘
                           │
          ┌────────────────▼────────────────┐
          │         Service Layer           │
          │  ┌────────────────────────────┐ │
          │  │      Spring Boot App        │ │
          │  │   ┌─────────────────────┐   │ │
          │  │   │   Controller层       │   │ │
          │  │   ├─────────────────────┤   │ │
          │  │   │   Service层         │   │ │
          │  │   ├─────────────────────┤   │ │
          │  │   │   Repository层      │   │ │
          │  │   └─────────────────────┘   │ │
          │  │   ┌─────────────────────┐   │ │
          │  │   │   Harness Agent     │   │ │
          │  │   │   (Skills/MCP)      │   │ │
          │  │   └─────────────────────┘   │ │
          │  └────────────────────────────┘ │
          └────────────────┬────────────────┘
                           │
          ┌────────────────▼────────────────┐
          │           Data Layer            │
          │  ┌─────────────┐ ┌─────────────┐ │
          │  │  PostgreSQL │ │    Redis    │ │
          │  │  (主从复制)  │ │  (缓存/会话) │ │
          │  └─────────────┘ └─────────────┘ │
          │  ┌─────────────┐ ┌─────────────┐ │
          │  │   MinIO     │ │   Milvus    │ │
          │  │  (对象存储)  │ │ (向量数据库) │ │
          │  └─────────────┘ └─────────────┘ │
          └─────────────────────────────────┘
                           │
          ┌────────────────▼────────────────┐
          │       External Services         │
          │  ┌─────────────┐ ┌─────────────┐ │
          │  │  DeepSeek   │ │   Harness   │ │
          │  │    API      │ │  Platform   │ │
          │  └─────────────┘ └─────────────┘ │
          └─────────────────────────────────┘
```

### 2.2 技术栈详细说明

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| **语言** | Java 21 | LTS版本，支持虚拟线程 |
| **框架** | Spring Boot 3.2.x | 社区成熟，生态完善 |
| **关系数据库** | PostgreSQL 16 | 主从复制，稳定可靠 |
| **ORM框架** | Spring Data JPA | 简化数据访问 |
| **缓存** | Redis 7.x | 会话存储、缓存层、限流计数 |
| **文件存储** | MinIO | 开源对象存储，支持分片上传 |
| **向量数据库** | Milvus 2.4.x | 高性能向量检索，IVF_FLAT索引 |
| **AI框架** | LangChain4J 0.30.x | Java版LangChain |
| **LLM** | DeepSeek API | 国产大模型，性价比高 |
| **Agent平台** | Harness Agent | 支持Skills和MCP模式 |
| **断路器** | Resilience4j | 保障系统稳定性 |
| **构建工具** | Maven | 依赖管理 |

### 2.3 项目目录结构设计

```
src/main/java/com/example/ragchat/
│
├── controller/                          # REST API控制层
│   ├── AuthController.java              # 认证接口
│   ├── FileController.java              # 文件管理接口
│   ├── DirectoryController.java          # 目录管理接口
│   ├── SearchController.java            # 搜索接口
│   ├── ConversationController.java      # 对话管理接口
│   ├── MultipartUploadController.java   # 分片上传接口
│   └── admin/AdminController.java       # 管理员接口
│
├── service/                             # 业务逻辑层
│   ├── UserService.java                 # 用户管理
│   ├── FileService.java                 # 文件管理
│   ├── DirectoryService.java            # 目录管理
│   ├── AuditLogService.java             # 审计日志
│   ├── FileValidationService.java       # 文件校验（P0）
│   ├── MultipartUploadService.java      # 分片上传（P1）
│   ├── CacheManager.java                # 多级缓存管理（P1）
│   └── impl/                            # Service实现类
│
├── serviceAi/                           # AI Service层（Harness架构核心）
│   ├── harness/                         # Agent核心
│   │   ├── RagChatAgent.java           # Agent接口
│   │   └── RagChatAgentImpl.java       # Agent实现
│   ├── intent/                          # 意图识别层
│   │   ├── IntentRecognizer.java       # 意图识别接口
│   │   ├── Intent.java                 # 意图对象
│   │   └── IntentType.java             # 意图类型枚举（13种）
│   ├── scheduler/                       # 技能调度层
│   │   └── SkillSchedulerImpl.java     # 调度器实现
│   ├── context/                         # 上下文管理层
│   │   ├── ContextManager.java         # 上下文管理器
│   │   ├── ConversationContext.java     # 对话上下文
│   │   └── UserPreferences.java        # 用户偏好
│   ├── skills/                         # 业务技能层
│   │   ├── FileSkill.java              # 文件操作技能
│   │   ├── SearchSkill.java            # 搜索技能
│   │   └── AISkill.java                # AI技能
│   └── mcp/                            # MCP适配层
│       ├── FileMCP.java                # MinIO文件存储
│       ├── SearchMCP.java              # Milvus向量检索（P1优化）
│       └── AIMCP.java                  # DeepSeek LLM调用
│
├── repository/                          # 数据访问层
├── entity/                              # 数据库实体
├── dto/                                 # 数据传输对象
├── config/                              # 配置类
│   ├── SecurityConfig.java              # 安全配置
│   ├── MilvusConfig.java                # Milvus配置（P1索引优化）
│   ├── RedisConfig.java                 # Redis配置
│   ├── DeepSeekConfig.java              # DeepSeek配置
│   ├── CircuitBreakerConfig.java        # 断路器配置（P0）
│   └── CacheConfig.java                 # 缓存配置（P1）
├── security/                            # 安全组件
│   ├── JwtTokenProvider.java            # JWT提供者
│   ├── JwtAuthenticationFilter.java    # JWT过滤器
│   └── RateLimitFilter.java             # API限流过滤器（P0）
├── exception/                           # 异常处理
│   ├── GlobalExceptionHandler.java      # 全局异常处理
│   └── BusinessException.java           # 业务异常
└── util/                                # 工具类
```

### 2.4 架构分层职责说明

| 层级               | 包名            | 职责                           |
| ---------------- | ------------- | ---------------------------- |
| **Controller** | `controller/` | 接收HTTP请求，参数校验，调用Service层 |
| **Service** | `service/` | 处理基础业务逻辑（CRUD、权限校验、文件校验、缓存管理） |
| **ServiceAI** | `serviceAi/` | AI智能业务编排（意图识别、技能调度、上下文管理） |
| **Repository** | `repository/` | 数据访问，与数据库交互 |
| **Entity** | `entity/` | 实体映射，数据库表结构 |
| **Config** | `config/` | 框架配置，Bean管理 |
| **Security** | `security/` | 认证授权、限流防护 |

### 2.5 ServiceAI层内部架构

`serviceAi/` 采用 **Harness AI架构**，内部包含完整的AI处理链路：

```
serviceAi/
│
├── harness/                      # Agent核心入口
│   └── RagChatAgent              # 接收请求，调度流程
│
├── intent/                       # 意图识别层
│   └── IntentRecognizer          # 识别用户意图（13种类型）
│
├── scheduler/                    # 技能调度层
│   └── SkillScheduler            # 根据意图路由到技能
│
├── context/                      # 上下文管理层
│   └── ContextManager            # 会话状态、用户偏好管理
│
├── skills/                       # 业务技能层
│   ├── FileSkill                 # 文件操作（上传/下载/删除/目录）
│   ├── SearchSkill               # 搜索（关键词/语义/高级）
│   └── AISkill                   # AI问答（单文件/多文件/总结/对比）
│
└── mcp/                         # MCP适配层
    ├── FileMCP                  # MinIO文件存储
    ├── SearchMCP                # Milvus向量检索（优化后）
    └── AIMCP                    # DeepSeek LLM调用（带熔断）
```

### 2.6 分层调用流程

```
HTTP 请求
    │
    ▼
┌──────────────┐
│ Controller   │  ← 接收请求，参数校验
└──────┬───────┘
       │ 调用
       ▼
┌──────────────┐
│  Service     │  ← 基础业务逻辑处理（权限校验、文件校验、缓存）
└──────┬───────┘
       │ 调用（AI场景）
       ▼
┌──────────────┐
│ ServiceAI    │  ← AI智能处理（意图识别→技能调度→MCP调用）
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Repository   │  ← 数据持久化
└──────┬───────┘
       │
       ▼
   数据存储（PostgreSQL/Milvus/MinIO）
```

***

## 3. Harness Agent设计

### 3.1 Agent架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Harness Agent                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Agent Core                             │  │
│  │  - 会话管理                                                │  │
│  │  - 技能调度                                                │  │
│  │  - 上下文管理                                              │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              │                                 │
│         ┌────────────────────┼────────────────────┐            │
│         ▼                    ▼                    ▼            │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│  │  FileSkill  │    │ SearchSkill │    │  AISkill    │        │
│  │  - 上传     │    │  - 关键词    │    │  - 问答     │        │
│  │  - 下载     │    │  - 语义      │    │  - 总结     │        │
│  │  - 删除     │    │  - 自然语言   │    │  - 分析     │        │
│  └─────────────┘    └─────────────┘    └─────────────┘        │
│         │                    │                    │            │
│         └────────────────────┼────────────────────┘            │
│                              │                                 │
│  ┌───────────────────────────▼───────────────────────────┐      │
│  │                      MCP Layer                        │      │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐      │      │
│  │  │ FileMCP     │ │ SearchMCP   │ │ AIMCP       │      │      │
│  │  │ (文件操作)   │ │ (检索服务)   │ │ (LLM调用)   │      │      │
│  │  └─────────────┘ └─────────────┘ └─────────────┘      │      │
│  └───────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Intent类型定义

| 意图类型 | 说明 | 对应技能 |
|----------|------|----------|
| FILE_UPLOAD | 文件上传 | FileSkill |
| FILE_DOWNLOAD | 文件下载 | FileSkill |
| FILE_DELETE | 文件删除 | FileSkill |
| FILE_LIST | 文件列表 | FileSkill |
| DIRECTORY_CREATE | 创建目录 | FileSkill |
| KEYWORD_SEARCH | 关键词搜索 | SearchSkill |
| SEMANTIC_SEARCH | 语义搜索 | SearchSkill |
| ADVANCED_SEARCH | 高级搜索 | SearchSkill |
| FILE_QA | 文件问答 | AISkill |
| MULTI_FILE_QA | 多文件问答 | AISkill |
| FILE_SUMMARIZE | 文件总结 | AISkill |
| ANALYZE_COMPARE | 分析对比 | AISkill |
| UNKNOWN | 未知意图 | - |

### 3.3 Agent核心组件职责

| 组件 | 职责 |
|------|------|
| **RagChatAgent** | Agent入口，处理用户请求，协调各组件 |
| **IntentRecognizer** | 解析用户输入，识别意图类型，返回置信度 |
| **SkillScheduler** | 根据意图类型路由到对应技能，执行技能调用 |
| **ContextManager** | 管理会话上下文、历史记录、用户偏好 |
| **Skill** | 业务技能封装，包含FileSkill/SearchSkill/AISkill |
| **MCP** | 外部系统适配层，封装MinIO/Milvus/DeepSeek调用 |

### 3.4 Agent工作流程

```
用户请求 → 请求解析(意图识别) → 技能选择(路由决策) → 技能执行(MCP调用) → 结果整合 → 返回响应
```

***

## 4. 数据库设计

### 4.1 ER图

```
┌─────────────────┐       ┌─────────────────┐
│     users       │       │   directories   │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │       │ id (PK)         │
│ username        │       │ name            │
│ email           │       │ parent_id (FK)  │──┐
│ password_hash   │       │ user_id (FK)    │──┼──┐
│ role            │       │ created_at      │  │  │
│ status          │       │ updated_at      │  │  │
│ failed_login    │       └─────────────────┘  │  │
│ locked_until    │                           │  │
│ created_at      │       ┌─────────────────┐  │  │
│ last_login_at   │       │      files       │  │  │
└────────┬────────┘       ├─────────────────┤  │  │
         │                │ id (PK)         │  │  │
         │                │ filename         │  │  │
         │                │ file_path        │◄─┼──┘
         │                │ file_size        │  │
         │                │ file_type        │  │
         │                │ directory_id(FK) │──┤
         │                │ user_id (FK)    │◄─┘
         │                │ version         │
         │                │ storage_key     │
         │                │ checksum        │
         │                │ is_deleted      │
         │                └────────┬────────┘
         │                         │
         │                ┌────────▼────────┐
         │                │  conversations  │
         │                ├─────────────────┤
         │                │ id (PK)         │
         │                │ user_id (FK)    │
         │                │ file_id (FK)    │
         │                │ title           │
         │                │ is_archived     │
         │                └────────┬────────┘
         │                         │
         │                ┌────────▼────────┐
         │                │  messages       │
         │                ├─────────────────┤
         │                │ id (PK)         │
         │                │ conversation_id │
         │                │ role            │
         │                │ content         │
         │                │ sources (JSON)  │
         │                └─────────────────┘
         │
         └───────────────►│  audit_logs     │
                          ├─────────────────┤
                          │ user_id (FK)    │
                          │ action          │
                          │ resource_type   │
                          │ ip_address      │
                          │ details (JSON)  │
                          └─────────────────┘
```

### 4.2 数据表设计

#### 4.2.1 用户表 (users)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | UUID | PK | 用户ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| email | VARCHAR(255) | NOT NULL, UNIQUE | 邮箱 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希(bcrypt) |
| role | VARCHAR(20) | NOT NULL, DEFAULT 'user' | 用户角色 |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'active' | 用户状态 |
| failed_login_attempts | INTEGER | DEFAULT 0 | 登录失败次数 |
| locked_until | TIMESTAMP | NULL | 锁定截止时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| last_login_at | TIMESTAMP | NULL | 最后登录时间 |

#### 4.2.2 文件表 (files)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | UUID | PK | 文件ID |
| filename | VARCHAR(255) | NOT NULL | 文件名 |
| original_filename | VARCHAR(255) | NOT NULL | 原始文件名 |
| file_size | BIGINT | NOT NULL | 文件大小 |
| file_type | VARCHAR(100) | NOT NULL | 文件类型 |
| directory_id | UUID | FK | 目录ID |
| user_id | UUID | FK, NOT NULL | 用户ID |
| storage_key | VARCHAR(255) | UNIQUE | MinIO存储键 |
| checksum | VARCHAR(64) | NOT NULL | SHA-256校验和 |
| current_version | INTEGER | DEFAULT 1 | 当前版本 |
| is_deleted | BOOLEAN | DEFAULT FALSE | 是否已删除 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.2.3 对话表 (conversations)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | VARCHAR(36) | PK | 对话ID |
| user_id | VARCHAR(36) | FK, NOT NULL | 用户ID |
| file_id | VARCHAR(36) | FK | 关联文件ID |
| title | VARCHAR(255) | NULL | 对话标题 |
| is_archived | TINYINT(1) | DEFAULT 0 | 是否已归档 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| last_message_at | DATETIME | NULL | 最后消息时间 |

#### 4.2.4 消息表 (messages)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | VARCHAR(36) | PK | 消息ID |
| conversation_id | VARCHAR(36) | FK, NOT NULL | 对话ID |
| role | VARCHAR(20) | NOT NULL | 角色(user/assistant) |
| content | TEXT | NOT NULL | 消息内容 |
| metadata | JSON | DEFAULT '{}' | 元数据 |
| sources | JSON | DEFAULT '[]' | 引用来源 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.2.5 审计日志表 (audit_logs)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | VARCHAR(36) | PK | 日志ID |
| user_id | VARCHAR(36) | FK | 用户ID |
| action | VARCHAR(100) | NOT NULL | 操作类型 |
| resource_type | VARCHAR(50) | NOT NULL | 资源类型 |
| resource_id | VARCHAR(255) | NULL | 资源ID |
| ip_address | VARCHAR(50) | NULL | IP地址 |
| details | JSON | DEFAULT '{}' | 详细信息 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.3 Milvus向量库设计

#### 4.3.1 向量集合结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| embedding | FloatVector(1024) | 向量嵌入 |
| file_id | VARCHAR | 文件ID |
| chunk_index | Integer | 分块索引 |
| chunk_text | VARCHAR | 分块文本 |
| user_id | VARCHAR | 用户ID（数据隔离） |
| created_at | LocalDateTime | 创建时间 |

#### 4.3.2 索引配置

- **索引类型**: IVF_FLAT
- **度量类型**: L2
- **nlist**: 1024（聚类数量）
- **nprobe**: 10（搜索时查询的聚类数）

***

## 5. API接口设计

### 5.1 接口分类

| 模块 | 路径前缀 | 说明 |
|------|----------|------|
| 认证 | `/api/v1/auth` | 登录、刷新Token、登出 |
| 文件管理 | `/api/v1/files` | 上传、下载、删除、列表 |
| 分片上传 | `/api/v1/files/upload` | 初始化、上传分片、完成 |
| 目录管理 | `/api/v1/directories` | 创建、删除、移动、树结构 |
| 搜索 | `/api/v1/search` | 关键词搜索、语义搜索 |
| 对话 | `/api/v1/conversations` | 消息发送、历史记录 |
| 管理员 | `/api/v1/admin` | 用户管理、审计日志、统计 |

### 5.2 关键接口设计

#### 5.2.1 认证接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/auth/login` | POST | 用户登录 |
| `/api/v1/auth/refresh` | POST | 刷新Token |
| `/api/v1/auth/logout` | POST | 用户登出 |

#### 5.2.2 文件管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/files/upload` | POST | 普通上传（单文件） |
| `/api/v1/files/upload/init` | POST | 分片上传初始化 |
| `/api/v1/files/upload/part` | POST | 上传分片 |
| `/api/v1/files/upload/complete` | POST | 完成分片上传 |
| `/api/v1/files/download/{fileId}` | GET | 下载文件 |
| `/api/v1/files/{fileId}` | DELETE | 删除文件 |
| `/api/v1/files` | GET | 获取文件列表 |

#### 5.2.3 搜索接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/search` | GET | 关键词搜索 |
| `/api/v1/search/semantic` | POST | 语义搜索 |

#### 5.2.4 对话接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/conversations` | POST | 创建对话 |
| `/api/v1/conversations/{id}/messages` | POST | 发送消息 |
| `/api/v1/conversations/{id}` | GET | 获取对话详情 |
| `/api/v1/conversations` | GET | 获取对话列表 |

#### 5.2.5 管理员接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/admin/users` | GET/POST | 用户列表/创建 |
| `/api/v1/admin/users/{id}` | GET/PUT/DELETE | 用户CRUD |
| `/api/v1/admin/audit-logs` | GET | 审计日志 |
| `/api/v1/admin/stats` | GET | 系统统计 |

### 5.3 公共响应结构

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": "2026-05-27T10:00:00"
}
```

***

## 6. 安全设计

### 6.1 认证机制

- **JWT Token**: 无状态认证，有效期2小时
- **Refresh Token**: 用于刷新Access Token，有效期24小时
- **密码策略**: bcrypt哈希（12轮），至少8位，包含大小写字母和数字
- **登录锁定**: 连续5次失败后锁定15分钟

### 6.2 权限控制

- **RBAC模型**: 基于角色的访问控制
- **用户隔离**: 数据查询自动添加用户ID过滤
- **最小权限原则**: 用户只能访问自己的数据
- **管理员权限**: 可访问所有数据，操作记录审计日志

### 6.3 P0级安全优化

#### 6.3.1 API限流（必须实现）

- **限流策略**: 基于IP地址，每分钟最多60次请求
- **封禁机制**: 超过阈值后封禁IP 15分钟
- **实现方式**: Redis计数器 + 过滤器

#### 6.3.2 文件类型校验（必须实现）

- **白名单**: PDF、DOC、DOCX、XLS、XLSX、PPT、PPTX、TXT、MD
- **双重校验**: 扩展名 + MIME类型
- **大小限制**: 单文件最大100MB
- **安全检查**: 防止路径遍历攻击

#### 6.3.3 断路器模式（必须实现）

- **熔断阈值**: 失败率超过50%时开启熔断
- **熔断时长**: 60秒
- **重试策略**: 最多3次，间隔1秒
- **限流控制**: 每秒最多10次LLM请求
- **并发限制**: 最多20个并发LLM调用

### 6.4 数据安全

- **传输加密**: HTTPS (TLS 1.3)
- **密码哈希**: bcrypt(12)
- **敏感数据**: 用户偏好、文件元数据加密存储
- **审计日志**: 保留90天，记录所有管理员操作

***

## 7. P1级性能优化

### 7.1 向量索引调优

- **索引类型**: IVF_FLAT（适合大规模数据）
- **nlist**: 1024（聚类数量，影响索引构建速度和检索精度）
- **nprobe**: 10（搜索时查询的聚类数，越大精度越高但速度越慢）
- **批量插入**: 每批100条，异步刷新
- **数据隔离**: 向量检索时自动添加用户ID过滤

### 7.2 分片上传支持

- **分片大小**: 50MB/片
- **断点续传**: 支持查询已上传分片状态
- **生命周期**: 初始化→上传分片→完成/取消

### 7.3 多级缓存策略

| 缓存类型 | 技术 | 有效期 | 适用场景 |
|----------|------|--------|----------|
| 本地缓存 | Caffeine | 5分钟 | 用户偏好、文件元数据 |
| Redis缓存 | Redis | 5-30分钟 | 搜索结果、向量嵌入 |
| 热点缓存 | Caffeine + Redis | 两级缓存 | 用户偏好 |

**缓存失效策略**:
- 用户数据变更时清除相关缓存
- 文件变更时清除文件元数据缓存
- 搜索结果缓存自动过期

***

## 8. 部署架构

### 8.1 容器化配置

使用Docker Compose进行本地开发和测试部署：

| 服务 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| postgres | postgres:16 | 5432 | 关系数据库 |
| redis | redis:7 | 6379 | 缓存、限流 |
| minio | minio/minio | 9000/9001 | 对象存储 |
| etcd | quay.io/coreos/etcd:v3.5.10 | 2379 | Milvus元数据 |
| milvus | milvusdb/milvus:v2.4.14 | 19530/9091 | 向量数据库 |
| ragchat-backend | 自定义构建 | 8080 | 应用服务 |

### 8.2 配置管理

- **环境变量**: 敏感配置通过环境变量注入
- **配置中心**: 生产环境建议使用Spring Cloud Config
- **密钥管理**: JWT密钥、API密钥使用密钥管理服务

### 8.3 健康检查

- **数据库**: PostgreSQL健康检查
- **缓存**: Redis ping检查
- **向量数据库**: Milvus healthz端点
- **对象存储**: MinIO健康检查

***

## 9. 监控与日志

### 9.1 监控指标

| 指标类型 | 内容 |
|----------|------|
| **性能指标** | 响应时间、QPS、吞吐量 |
| **资源指标** | CPU、内存、磁盘、网络 |
| **业务指标** | 文件上传量、搜索次数、问答请求数 |
| **错误指标** | 错误率、熔断次数、限流次数 |

### 9.2 日志管理

- **日志级别**: DEBUG/INFO/WARN/ERROR
- **日志格式**: JSON格式，便于日志分析
- **保留期限**: 操作日志90天，系统日志30天
- **日志聚合**: ELK或Loki

***

## 10. 核心业务流程

### 10.1 文件上传流程

```
用户选择文件 → 文件校验（类型/大小）→ 上传到MinIO → 解析提取文本 → 文本分块(512tokens) → 
生成向量嵌入 → 存入Milvus（带用户ID）→ 更新文件元数据 → 返回上传结果
```

### 10.2 语义搜索流程

```
用户输入查询 → 生成查询向量 → 向量数据库检索（用户ID过滤）→ 相似度排序 → 返回相关文件列表
```

### 10.3 AI问答流程

```
用户选中文件+提问 → 获取文件向量 → 检索相关内容块 → 构建RAG Prompt → 调用LLM（带熔断）→ 
生成回答 → 保存对话记录 → 返回回答（带来源标注）
```

***

## 附录：优化项优先级汇总

| 优先级 | 优化项 | 描述 |
|--------|--------|------|
| P0 | API限流 | 防止恶意请求攻击 |
| P0 | 断路器模式 | 保证系统稳定性 |
| P0 | 文件类型校验 | 防止恶意文件上传 |
| P1 | 向量索引调优 | 提升检索性能 |
| P1 | 分片上传 | 支持大文件上传 |
| P1 | 缓存策略 | 减少重复计算 |
